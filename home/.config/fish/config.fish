set -x PATH $PATH ~/bin
set -x PATH $PATH ~/.n/bin
set -x PATH $PATH ~/.cargo/bin
set -x PATH $PATH ~/.local/bin

set -x fish_greeting

alias c="git commit"
alias s="git status"
alias co="git checkout"
alias d="git diff -w"
